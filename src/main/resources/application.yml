spring:
    application:
        name: 'chat'
    jpa:
        hibernate:
            ddl-auto: 'create-drop'
    session:
        store-type: 'redis'
    data:
        redis:
            repositories:
                enabled: false
